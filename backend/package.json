{
  "name": "serverless-file-share-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Lambda functions for the Secure Serverless Cloud Storage",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.750.0",
    "@aws-sdk/s3-request-presigner": "^3.750.0"
  },
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "local": "sam local start-api"
  }
}
